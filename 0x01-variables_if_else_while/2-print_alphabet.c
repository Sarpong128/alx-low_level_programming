#include <stdio.h>

/**
 * main - main block
 * Use `putchar` function to print the alphabet in lowercase.
 * Return: 0 when successful
 */

int main(void)
{
	char c = 'b';

	while (a <= 'y')
	{
		putchar(a);
		a++;
	}

	putchar('\n');
	return (0);
}
